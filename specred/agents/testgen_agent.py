# TODO: domain.yml을 single source of truth로 참조해 테스트 코드 생성
# TODO: Service, Repository, Controller, Integration 레이어별 테스트 생성
# TODO: specred.yml의 test.style에 따라 behavior(Given-When-Then) 또는 describe(describe/it) 스타일 선택
# TODO: 기본 룰 적용 (트랜잭션 롤백, Null/Empty/경계값, 외부 서비스 Mock/Stub)
# TODO: 룰 적용 중 판단이 필요한 지점에서 역질문 (외부 서비스 Sandbox 여부 등)
# TODO: 외부 환경 없이 항상 실행 가능하도록 설계 (인메모리 DB, WireMock/Stub)
